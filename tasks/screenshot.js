import screenshot from 'screenshot-desktop';

/**
 * capture device screenshot
 * 
 * NOTE: can only be captured on the bot host device, i.e. mine @author Cody
 */
module.exports = {
	name: 'screenshot',

	execute(message, args) {
		
	}
} 